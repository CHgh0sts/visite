# PostgreSQL (ne commitez pas le vrai mot de passe)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Secret pour POST /api/scene-interactions (publier depuis l’éditeur)
# Générez une chaîne longue aléatoire ; même valeur côté serveur uniquement.
SCENE_INTERACTIONS_WRITE_SECRET=""
